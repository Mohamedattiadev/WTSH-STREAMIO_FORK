# Continue the Ember Rail → WTSH redesign: remaining fixes

## READ THIS FIRST — next session's task list (9 items, in priority order)

The user's own words for each item are kept verbatim in quotes so intent isn't lost in paraphrase. They attached real screenshots (both of the mockup and of the live app) for every item below — a fresh session won't have those images, so each item below carries a from-source description precise enough to work from without them: exact mockup file/line pointers, exact app file pointers, and what was already confirmed live this session. Work through these **in order**, verify each live on `localhost:8080` **signed in** before moving to the next (see Verification standard below), commit as you go. The user explicitly asked: *"i want u check each part in the article and take screenshot of it analyse and check the app if not similar and fix it make it identical"* — treat `design/ember-rail-mockup.html` as the literal source of truth for every item that has a real mockup counterpart, not memory or approximation. Don't silently skip or scope-cut anything — if something seems ambiguous or has no real data behind it, **ask the user**, exactly like the "4K HDR badge" and testimonials-row calls made earlier in this project.

### 1. Board hero is missing the "What people are saying" reviews row (`"the artical's hero has these comments what people says but ours not"`)
**Do not build this without asking the user first.** The mockup's hero (`design/ember-rail-mockup.html` around line 1432-1451, `.row-head><h3>What people are saying</h3>` + `.review-row` / `.review-card`) shows 3 star-rated quote cards ("— via Letterboxd", "— via Rotten Tomatoes", "— via Reddit") under the hero. This is **100% hardcoded fabricated text** in the mockup for its own fake demo show ("The Signal Between Us") — there is no real reviews/testimonials data source anywhere in stremio-core, Cinemeta, or any addon manifest surfaced in this codebase. This is the exact same category of thing as the "4K HDR" badge this project already correctly declined to fabricate earlier in the session (see the ground rules below and prior git history). Ask the user: is there a real reviews API/addon they want wired in, or should this section be skipped? Don't invent star ratings or quotes.

### 2. Sidebar collapse toggle still hard to see (`the sidebar icon toggle icon`)
This was already touched twice this session — first a contrast-only pass (commit `e0222e9`, prior session), then a real clipping-bug fix this session (commit `fdaf236c`: `.rail-toggle` in `src/components/NavBar/VerticalNavBar/styles.less` was positioned at `right: -0.85rem`, half outside the rail, and the rail's own `overflow-x: hidden` was silently clipping that overflowing half away — moved it fully inside the rail's bounds instead). The user is flagging it again after that fix, so there's more to it. Take a **fresh** screenshot of the collapsed rail at rest (no hover) signed in on a real page before touching anything — don't assume the earlier fix was insufficient without confirming exactly how. Likely next step: the resting background/border contrast is still too close to the rail's own dark background (`background-color: var(--surface-elevated-color)` may not read as different enough from `--surface-ink` at a glance) — consider a stronger fill (e.g. the ember accent at low opacity, or a visible ring at all times not just on hover) rather than another position tweak.

### 3. Catalog/discover cards don't match the mockup's card design (`"the cards in the mocup make like it bro"`)
The referenced mockup card (self-contained: backdrop image, "SERIES · 2021" eyebrow label + star rating, bold title "Signal & Noise", genre pills "Drama / Tech / 3 seasons", one-line description, and a full 3-button row "Play / Trailer / + Library" *all inside the card itself*) is **structurally different** from our current `MetaItem` grid tile (poster + title + subtitle + rating only, with full info/actions living separately in the side `MetaPreview` panel). Before changing anything: **grep the mockup for this exact card's markup** to find out which real page/section it belongs to (it doesn't obviously match Discover's plain grid — check if it's a "Trending"/featured row treatment, a Search-result list-card, or something else entirely) so you're comparing the right two things. Don't retrofit `MetaItem` to look like this if the mockup only uses this treatment in one special context (e.g. a hero-adjacent recommendation row) — match scope to scope.

### 4. Trailer modal: match the mockup's chrome, but wider (`"the trailer in the mockup pls do like it but make its width a bit bigger"`)
Mockup reference: `design/ember-rail-mockup.html` line ~651-699, `.trailer-modal-inner { max-width: 28rem; }`. Our own `TrailerModal` (`src/components/TrailerModal/`) was just fixed this session (commit `d06f3970`) — it turned out its whole style override block was silently a no-op due to a CSS Modules cross-file scoping bug (missing `:import()`, same issue MetaItem/EventModal already solve correctly), so it had been quietly falling back to bare default width/padding all session; that's fixed now and it's at `width: 30rem`. **The user wants it a bit *wider* than that** — note the mockup's own reference is actually *narrower* (28rem) than our current 30rem, so "wider" here means wider than our current state, not matching the mockup's literal number. Try ~34-36rem, but re-verify live at a real windowed (non-fullscreen) browser height that it still needs **no internal scroll** (this was the whole point of the narrowing fix — a wider modal means a taller 16:9 video area too, so you may need to trim `.info-container` padding or the description's `-webkit-line-clamp` further to compensate). Keep the current autoplay-real-video behavior (a deliberate earlier decision, documented in `TrailerModal.js`'s own comments, over the mockup's static click-to-play thumbnail — real playable video beats a fake preview) — this item is about sizing/chrome parity, not reverting that.

### 5. Chat is broken (`"chat not working at all"`)
Confirmed live this session — two distinct real bugs, not one:
- **It's not actually dead, it's just extremely slow and reads as stuck.** Sending a message triggers a fan-out `search` request across **every installed addon** (this test account has ~16) before `/api/chat` is ever called — confirmed via network logging: `v3-channels.strem.io` 500s, a `tmdb-addon.baby-beamup.club` mirror fails with `ERR_FAILED`, several other slow third-party addons, and the actual `POST /api/chat` doesn't fire until **~20 seconds** after send. A real user gives up long before that. This is core's own multi-addon `search` model (see `useChatSession.js`'s `search.catalogs`, `retrieval.js`) — not something to "fix" by skipping addons, but the UX needs to show real progress ("Searching your addons…") instead of a bare `...` typing dot that looks identical whether it's been 1 second or 20.
- **Duplicate send**: sending one message once produced **two** identical user bubbles in the transcript with only one assistant reply. Reproduced live (screenshot evidence: two "Recommend me a good sci-fi movie" bubbles from a single Enter press). Start at `src/routes/Chat/ChatPanel/ChatPanel.js` (`TextInput onSubmit={onSubmit}` + a separate send `Button onClick={onSubmit}` — check `TextInput`, `src/components/TextInput/`, for a possible internal double-fire on Enter) and `useChatSession.js`'s `sendMessage`.
- **Answer quality**: the eventual reply was "I couldn't find any sci-fi movies in your installed addons" despite several catalogs returning real 200 data for the search - worth checking whether `retrieval.js`'s matching logic handles a genre-style ask ("a good sci-fi movie") correctly versus treating it as a literal title-text search (it appears to search for the literal string `"Science Fiction"` as if it were a title).

Repro: open Chat signed in, type any recommendation request, send once, watch network tab and the transcript.

### 6. ChatFab doesn't look as clean as the mockup's (`"look how clean this one is in our app not like this"`)
Compare `src/components/ChatFab/` (`ChatFab.js` + `styles.less`) against the mockup's own chat FAB (search the mockup HTML/CSS for its floating chat button - the reference screenshot shows a small, saturated red/orange circle with a chat-bubble icon and a green "online" dot badge, clean and vibrant with no visual noise). Screenshot both side by side before making changes.

### 7. Addons page doesn't match, and the "Hub" tab is missing entirely (`"the addson not same bro fix, and the hub tab missing"`)
Mockup reference: `design/ember-rail-mockup.html` line ~1710-1755. The tab row is **five** tabs — `My <4> / Official <62> / Community <318> / Regional / Hub` — our real `src/routes/Addons/Addons.js` only renders four (`My/Official/Community/Regional`, see the `.tabs-container` block around line 152-183); **Hub is missing outright**. The mockup also shows a "From the Addon Hub" featured-install strip above the regular installed-addon list (`.hub-section`, 4 cards with logo/name/description/Install button, plus a "Fetched live — no need to open the Hub" live-status pill). **The 4 example addon names in the mockup (RPDB Ratings, AnimeKitsu, 4K Debrid Hub, Local Files Plus) are mockup placeholder examples, not a spec to copy verbatim** — before building anything, check whether this project already has real groundwork for a Hub/regional addon aggregator: `src/routes/Addons/CONSTANTS.js` and `src/routes/Addons/RegionalHub/RegionalHub.js` have real, uncommitted, in-progress work from another session (a curated list of real regional addon manifests) sitting in the working tree right now — read it fully before assuming you need to build Hub support from scratch, and don't discard that work. Research whether there's a real, existing addon-hub aggregator API (Stremio has a real community addons-hub service) before fabricating install cards; if no real source is reachable, ask the user rather than inventing addon names/descriptions.

### 8. Player shouldn't be fullscreen — embed it in the app shell like the mockup (`"the player looks good but should be not fully screen like the one in the article's screenshot"`)
**This is the single biggest, riskiest item on this list — plan it carefully, and confirm the approach with the user before a large rewrite.** The user is explicit: they like the *current* player's own controls/icons (confirmed this session: skip/play controls, network/speed/cast/subtitles/audio/search/chat/aspect-ratio/streaming-server/options icons, the download button just added) — this is **not** about redesigning the control bar again. It's specifically that our real player (`src/routes/Player/Player.js`) is a fullscreen immersive takeover route with no rail/topbar, while the mockup's player (`design/ember-rail-mockup.html` line 1891, `data-page="player"`) is **architecturally just another `.page-view`** inside the exact same shell every other route uses — rail and top search bar stay visible, the video sits in the normal content area, and a real info panel (title/rating/genre tags/description) sits directly below the player, scrollable, not in a menu. The mockup still keeps a fullscreen toggle (a `⛶` icon top-right) for when the user actually wants to go fullscreen — so the ask is "embedded by default, fullscreen on request," not "remove fullscreen." This is exactly the structural change a prior round of this same session explicitly declined to attempt (see the "Even later round" journal below, item 6's original commit notes: *"a full structural rewrite would be high-risk for low real benefit"*) — the user is now overriding that call, so it needs to happen, but given the size (moving a mature, gamepad-nav/casting/keyboard-shortcut-heavy component out of a dedicated fullscreen route and into the shared shell) this deserves its own planning pass (Plan mode) rather than diving straight into edits.

### 9. Settings cards don't fill the screen, leaving dead space (`"make the cards of settings fit the screen so no empty space"`)
**Root cause already found, not just a hunch**: `src/routes/Settings/components/Section/Section.less` line 3 has `max-width: 38rem;` on the shared card wrapper every Settings section uses. The mockup's own equivalent (`design/ember-rail-mockup.html` line 1160, `.settings-body { flex: 1; ... min-width: 0; }`) has **no max-width at all** — its cards fill all available width next to the fixed `10.5rem` menu column (line 1156). Fix is likely as simple as removing or substantially raising that `38rem` cap so cards actually use the available width (verify it still reads well at both narrow and very wide viewports — a bare `flex:1` with no cap could make long rows like the account email/action-link list feel overly stretched on an ultrawide monitor, so consider a more generous but still-bounded max-width, e.g. matching the mockup's actual rendered proportion, rather than blindly deleting the constraint).

## Ground rules for all of the above
- **Never fabricate data.** Item 1 (reviews) and the addon names inside item 7 (Hub cards) are the two live tripwires this round — stop and ask rather than invent plausible-looking content, exactly as this project already did for the "4K HDR" badge earlier in the session.
- **Verify everything live, signed in.** Playwright + a real browser binary works well in this sandbox: `/opt/brave-bin/brave` via `chromium.launch({ executablePath: ... })` (plain `npx playwright install`'s bundled Chromium has had version-mismatch issues before). To get a signed-in session covering BOTH the Stremio account (Library/Calendar/Addons state) and the separate Supabase account ("Watch Reminders & Chat Sync", needed for Chat/Calendar reminders): Settings → General → real email/password **Sign up** for the Stremio side (any disposable email, e.g. `@mailinator.com`), then separately click "New here? Sign up" under the "Watch Reminders & Chat Sync" card in the same Settings page for the Supabase side. A combined session covering both was saved to `/tmp/pw-check/combined-session.json` in the prior session's sandbox — check if it's still present and not expired before recreating one from scratch.
- **Watch for concurrent edits.** Other sessions have touched this same repo mid-session before (`iconMap.ts`, `Search.js`, and the still-uncommitted `Addons/CONSTANTS.js`/`RegionalHub.js` regional-addon work) — re-read files fresh before editing, and don't revert or fight changes that look deliberate just because you didn't make them.
- **Dev server discipline**: `fuser -k 8080/tcp` before restarting `pnpm start` — never leave two instances running (a prior incident here caused a real OOM that killed the user's other apps). Check `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080` first — a healthy dev server from a previous session may already be running and reusable.
- **Commit as you go**, small and focused, matching this session's established pattern (see `git log` for style/tone).
- **Checkpoint with the user after a meaningful chunk** — this list has one genuinely large, risky item (8) mixed in with several small ones; don't let it swallow the whole session silently. Item 8 in particular should get an explicit go-ahead on the approach (Plan mode output, or at minimum a short written plan) before large-scale edits.

## Even later round (prior session, commits after the streaming-server/calendar/trailer round below)

Commits `350d542f..5c99aae6` on `development`, in order — this is the direct predecessor to the 9-item list above, itself following up on the original 5-item list further below:

1. **Icon sizing fixed for real** (`350d542f`) — `ActionsGroup.less` and the standalone Share `ActionButton` were still 4rem circles / ~1.75-2rem icons; shrunk to ~2.75rem/1.15rem to match the pills.
2. **Player logo/CenterControls collision fixed** (`d0b8b49d`) — `CenterControls` now only renders once buffering has cleared and there's no error, mirroring `Buffering`'s own visibility condition so the two can never overlap. Verified against a real stuck-buffering Torrentio stream.
3. **Streaming-server switching + auto-fallback** (`1663201e`) — new `StreamingServerMenu` reachable via a player control-bar button, reusing the same `Ctx`/`UpdateSettings` dispatch Settings' existing `URLsManager` uses; auto-fallback watches the real `streamingServer.settings.type === 'Err'` signal and tries the next configured URL once per stream. Verified live end-to-end (manual switch to a broken URL → detected → fell back automatically with a toast).
4. **Real "Add to Calendar" button** (`b57dd168`) — new `AddToCalendarButton` wired into `MetaPreview`'s action row and every `MetaItem` catalog card, reusing the real `useCalendarReminders(user).addReminder()` path. Had to portal the date-picker popover to `document.body` (`src/components/AddToCalendarButton/AddToCalendarButton.tsx`) because MetaItem cards clip overflow on both `.poster-container` and `.poster-actions` for the poster's rounded corners, which silently sliced an in-place popover to a sliver.
5. **3-dot card menu removed, action-row wrapping fixed, Library preview enriched** (`e22068a8`) — dropped `Multiselect` from `MetaItem`'s poster corner per explicit feedback (the only actions it carried - play/details/watched/dismiss/remove - stay reachable via the detail page and preview panel); `MetaPreview`'s compact action row used `justify-content: space-between`, which centered/orphaned a lone wrapped button once the calendar button was added - switched to `flex-start` + `gap`; Library's preview panel had no Trailer button and no genres/cast because `LibraryItem` (`core/types/LibraryItem.d.ts`) is a genuinely thin model - fixed the same way Calendar's `VideoPreview.tsx` already solved this, by re-fetching full `MetaDetails` on demand via `useMetaDetails`.
6. **TrailerModal's real CSS Modules scoping bug fixed** (`d06f3970`) — see item 4 above for the full story; this is the one to remember if trailer styling ever looks like it's "not applying."
7. **Calendar reminders now show on their day squares** (`2b2a3626`) — new `ReminderPreview` panel opens on click (poster/title/full date/remove), reachable from both the day-square marker and the existing sidebar list. Deliberately not a full `MetaPreview` re-fetch like `VideoPreview.tsx` - `calendar_events` never stored a real deep link, only `{title, poster_ref, scheduled_date, source}`, so there's no id to fetch full `MetaDetails` from without fabricating one. **A real DB migration (adding a `deep_link` column to `calendar_events` so reminders could show full real movie info) was attempted and blocked by the permission system** - if the user wants that, it needs an explicit go-ahead; `supabase/schema.sql` is the source-of-truth file to update alongside it, and `POSTGRES_URL_NON_POOLING` in `.env` is the working `psql` connection string (the plain `POSTGRES_URL` has a `?supa=` query param `psql` rejects).
8. **Sidebar toggle clipping fixed** (`fdaf236c`) — see item 2 above.
9. **Stream row redesign + per-stream auto-fallback** (`108345f9`) — `-webkit-line-clamp` was set on a `display: flex` container with `overflow-y: visible`, so it never actually truncated, and `white-space: pre` (not `pre-line`) blocked wrapping entirely - one long raw addon description line would run past the card. Fixed the box model, and split the description on its first newline into a bold title line + muted meta line. Also added auto-fallback to a **different stream/source** (not the same thing as item 3's streaming-server-URL fallback) when the one actually playing fails outright (`video.events` `'error'` with `.critical`) - lazily fetches the full cross-addon streams list via `useMetaDetails` only after a real failure, and tries the next untried stream. Couldn't force a genuine critical playback error in this sandbox to verify the fallback fires end-to-end (no real streaming server, torrent streams just hang in buffering rather than erroring) - confirmed it compiles clean and the player behaves normally with the new lazy fetch present and dormant, but this is unverified live and worth a closer look if the next session finds a way to force a real error.
10. **First-class Download button in the control bar** (`5c99aae6`) — was buried in the right-click `OptionsMenu` as "Download this video"; added to `ControlBar`'s main row, reusing the same real `stream.deepLinks.externalPlayer.download` field, only shown when a stream actually has one.

Investigated but not fixed / found to already work: could not reproduce a real "3 tabs active at once" bug in either the sidebar (`VerticalNavBar`) or the Addons page's own tab row after several targeted Playwright tests — both tracked correctly across rapid tab switches. If it's still happening for the user, it may need a screen recording or the exact click sequence to pin down.

## Latest round (this session, commits `d734702..4c83555`)

1. **Trailer now opens as a real popup** instead of navigating to the full Player route (`src/components/TrailerModal/` — new; wired into both `MetaItem.js`'s card-hover pill and `MetaPreview.js`'s panel button). Uses the real `trailerStreams[0].ytId` YouTube id via a plain iframe embed — confirmed the field survives through stremio-core rather than being fabricated.
2. **Calendar now has a real preview panel** (`src/routes/Calendar/VideoPreview/` — new), matching Discover/Search's richness. Calendar's own `CalendarContentItem` model is deliberately thin (no description/genres/cast), so this re-fetches the real full `MetaDetails` on demand via `useMetaDetails`, keyed off the item's own real deep link (parsed with `routesRegexp.metadetails.regexp` + `toPath` to strip the leading `#`).
3. **Stream list rows redesigned** (`src/routes/MetaDetails/StreamsList/Stream/styles.less`) to match the app's card language — bordered card, ember-gradient addon-name badge, persistent-but-subtle play icon. Deliberately did NOT attempt to parse seeder/size/source out of raw addon description text — that's addon-specific free text with no reliable cross-addon structure.
4. **Sidebar active-tab indicator fixed** (`VerticalNavBar.js`) — it was translating only on the Y axis, so the left accent bar sat at the rail's outer edge instead of the selected pill's actual left edge (the pill is horizontally centered in the collapsed rail via `margin: 0 auto`, leaving a gap the indicator never accounted for). Now translates X too, using `activeItem.offsetLeft`.
5. **Catalog cards now show the real IMDb star rating** (`MetaItem.js`) matching the mockup — the data was already flowing to the side preview panel (`MetaPreview` reads it from the item's `links` array, category `imdb`), just never passed down to the individual grid-card components in `Discover.js`, `Search.js`, and `Chat/ChatPanel.js`. Added the same extraction to `MetaItem` and wired the missing `links` prop through at each call site. Added a `star` icon to `Icon/iconMap.ts` (lucide didn't have one mapped).
6. **Player controls redesigned** to mirror the mockup: a floating center play/pause button flanked by skip ±10s buttons over the video itself (`src/routes/Player/CenterControls/` — new; confirmed live that clicking it never double-toggles playback, since `Video` and this overlay are separate sibling subtrees, not nested — clicks never bubble from one into the other), and the playback-speed control is now a readable "1x"-style chip instead of a bare icon (`ControlBar.js`). Did **not** attempt the mockup's "player as an embedded page with the rail/topbar still visible" structure, and did not add a fake "quality/4K" chip — the real player is a mature, fully-featured immersive fullscreen component (casting, gamepad nav, keyboard shortcuts, statistics, hardware transcoding) and there's no real adaptive-quality data to back a quality chip; a full structural rewrite would be high-risk for low real benefit versus this additive, visually-aligned approach. **(Superseded by item 8 at the top of this document — the user has since asked for exactly this structural change.)**

Search bar and the catalog "card" panel styling were both re-checked directly against the mockup this session and found to already match (flat underline search field; `MetaPreview` panel structure) — no changes needed there.

## Even later round (same session, commits after `4c83555` — through the Add to Calendar button)

7. **Trailer modal chrome cleaned up**: stripped YouTube's own scrubber/CC/settings/branding via embed params (`controls=0&modestbranding=1&rel=0&iv_load_policy=3&disablekb=1`) since none of that ever showed in the mockup's own trailer preview. Also fixed a real CSS specificity bug where the description-to-button gap wasn't closing — `ModalDialog`'s base padding/margin rules are nested 4 classes deep, so a shallower override was silently losing the cascade; settled with `!important`.
8. **Account dropdown menu (`NavMenuContent`) redesigned** — this component had never been touched by the whole redesign; icons were still 2rem at 0.3 opacity (oversized/washed out) and Log In/Sign Up + Log Out were both plain underlined text with zero button styling — that was the actual "login button not visible" bug, not a rendering issue. Now has properly-sized icons and real pill buttons (filled ember-gradient primary for Log In/Sign Up, bordered secondary for Log Out).
9. **Fixed the sidebar's visible slide-in glitch on every tab click** — every tab switch remounts `VerticalNavBar` fresh (each top-level tab route shares one router view-slot with its siblings, so switching tabs literally mounts a new instance), and while the indicator's position was actually computed correctly immediately, the CSS transition still visibly animated it in from its default untransformed state every time — read as the indicator sliding in from the top-left on every click. Fixed by disabling the transition for just the first positioning after a fresh mount.
10. **Calendar gained a real manual "add reminder" button** — previously the *only* way to schedule something was through Chat's natural-language parsing. `useCalendarReminders.addReminder(title, date, source='manual')` already existed and clearly anticipated this path but was never wired to any UI. Added a "+" next to "Your Reminders" opening a small title+date form; verified live end-to-end against a real Supabase-backed account.
11. **Fixed the search bar not filling the top bar** — a leftover `max-width: 30rem` left a large dead gap before the bell/avatar; the mockup's own `#topSearch` has no cap at all.
12. **Reduced the labeled action pills' (Show/Trailer/+Library) icon size and un-forced their height** — they'd inherited a `height: 4rem` / `1.75rem`-icon sizing meant for icon-only circular buttons, making them read as oversized next to their own text.

## Context

This repo (`streamio-ati-version`) is a Stremio web-client fork being redesigned into a new dark/ember-themed app now called **WTSH** (renamed from "WTS"). The visual/interaction source of truth is `design/ember-rail-mockup.html` — read it directly (HTML + `<style>` + `<script>`) before touching any styling; don't guess at colors/spacing from memory or descriptions.

Run the app locally with `pnpm start` (webpack-dev-server on `http://localhost:8080`). Respect the existing project convention: never leave more than one `pnpm start` running — always `fuser -k 8080/tcp` and confirm the port is free before restarting.

**Verification standard for this whole task**: don't just read code and assume it's fixed. For every change, actually load the affected page at `localhost:8080` (Playwright + a real browser binary, `/opt/brave-bin/brave` via `chromium.launch({ executablePath: ... })`) and confirm visually, signed in. Several bugs across this project's history only became obvious under a real signed-in session with real catalog data — anonymous/empty-state testing hides a lot.

## What's already done (for context — don't redo)

Everything in the three "round" journal sections above, plus the original foundation work summarized here:
1. Full Ember Rail redesign (shell, all pages, Chat with real Gemini LLM backend, Supabase persistence).
2. `api/chat.js` re-hosted on the webpack dev server so Chat's LLM path works under plain `pnpm start`.
3. Settings' account links restyled to the mockup's chevron list-row pattern.
4. Chat's scheduling extraction and a server-timezone bug fixed; Calendar's `Reminders` section added (read side of an already-working write path).
5. Chat FAB / bottom-tab-bar overlap fixed on mobile.
6. Account email overflow fixed on narrow Settings viewports.
7. Addons' mobile "Add addon" button fixed (was obscuring card titles).
8. **Major**: fixed wildly inconsistent card sizes across Discover/Library/Search grids (`repeat(N, 1fr)` → `repeat(N, minmax(0, 1fr))`) and Board's `MetaRow` carousels (flex `min-width: auto` default).
9. Account/notifications popup stacking-context z-index bug fixed.
10. Renamed the app from WTS to WTSH throughout.
11. Sidebar collapse/expand toggle contrast, active-tab indicator transition speed, top search bar pill→flat-underline restyle.

A known, investigated, low-priority router detail: the router keeps a previous route's `MainNavBars`/`VerticalNavBar` instance mounted alongside the current one briefly (bounded at ~2 instances) — a page-wide DOM query can grab the stale instance, but the currently-visible route's own indicator tracks correctly. Don't refactor the router over this unless it's shown to cause a real visible symptom.
